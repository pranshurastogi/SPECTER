{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "cargo build --release -p specter-cli"
  },
  "deploy": {
    "startCommand": "./target/release/specter serve --port $PORT",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  }
}